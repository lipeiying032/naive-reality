# binaries
*.exe
*.test
/release-windows/
/release-linux/
*.zip
*.tar.xz

# naive kernel / upstream (fetched at build time)
/naive.exe
/upstream/
/src/
# h3native: the QUICHE checkout is fetched at build time, not vendored
/quiche/
